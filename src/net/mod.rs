// use std::{collections::HashMap, sync::Arc};

// use lazy_static::lazy_static;
// use tokio::sync::RwLock;

// use crate::object::client::BitServer;

pub mod quic;

// lazy_static! {
//     /// 记录ServerID的内容
//     pub static ref SERVER_POOL: Arc<RwLock<HashMap<u32, BitServer>>> = Arc::new(RwLock::new(HashMap::new()));
// }
